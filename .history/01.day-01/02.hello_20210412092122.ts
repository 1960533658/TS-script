let num = 12;
let num2: Number;
// num2 = "nihao"

/**
 * 
 * @param a any 任何数据
 * @param b 数字
 */
function aler(a:any[], b:number) {
  console.log(a.length+10);
}
// aler([1,2],"23")

// 当固定了类型之后，传入或者赋值的类型不符合数据限制时，会直接报错（不辨译