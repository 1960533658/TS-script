/**
 * & 声明文件
 * - 对于常用的第三方库,其实已经有大神帮我们变好了响应的声明文件,所以在企业开发
 *   中,如果我们需要使用一些第三方JS库的时候我们只需要安装别人写好二的声明文件即可
 * - TS声明文件的规范 @type/xxx
 *    例如:如果想要安装jQuery的声明文件,那么只需要npm install @types/jquery即可
 */

// 在main.js中


// console.log(("#app").height());

// 自动生成tsconfig.json文件 npm  --init
